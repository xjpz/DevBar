# Codex 项目规则

开始任务前先读取 `.codex/project.yaml`，并以其中当前技术栈、所有权、验证命令和风险边界为准。

- 使用“诊断 / 方案 / 实施 / 提交 / 发布”五种工作模式，不跨越未授权边界。
- 保留现有脏工作区；禁止自动 reset、clean、回滚、全量暂存或覆盖无关改动。
- 中文需求的设计文档默认使用中文；需要审批时停在设计门。
- 修改后运行 `./scripts/codex-verify` 的相应档位，并区分代码失败、环境阻塞和未验证项。
- 只有明确要求“代码提交”才提交；推送、发布、公证、上传和生产变更均需分别授权。
